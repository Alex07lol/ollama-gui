[package]
name = "ollama-plugins"
version = "0.1.0"
edition = "2021"
description = "Plugin subsystem loader engine for Ollama GUI"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
wasmer = { version = "4.0", optional = true } # WebAssembly extensibility
